# exam_tool

## Frontend (use bun)

Install and run the frontend with bun:

```bash
cd Source/frontend
bun install
bun run dev    # development
bun run build  # production build
bun run preview
```

This project uses `bun` as the JavaScript runtime/package manager for the frontend. Add a `bun.lockb` by running `bun install` locally.