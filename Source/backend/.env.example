PGHOST=localhost
PGPORT=5432
PGUSER=
PGPASSWORD=
PGDATABASE=
DB_MODE=auto
DOCKER_AUTO_START=true

AUTH_EMAIL=admin@talland.nl
# Seed password for app_auth table (used on first startup when table is empty)
AUTH_PASSWORD=admin123
JWT_SECRET=
JWT_EXPIRY_MINUTES=480
